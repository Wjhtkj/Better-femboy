{
  "name": "femboy-test",
  "version": "1.0.0",
  "private": true,
  "description": "男娘潜质综合评估量表 - Cloudflare Pages 静态站点",
  "scripts": {
    "test": "node tests/language-config.test.js && node tests/translation-template.test.js && node tests/en-us-translation-completeness.test.js",
    "dev": "wrangler pages dev . --d1=DB",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
